# Optional: use a normal sibling clone while editing handbook content.
# FIELDBOOK_CONTENT_DIR=../agentic-engineering

# Optional: inspect a fetched release tag instead of the checked-out revision.
# FIELDBOOK_CONTENT_REF=fieldbook-v0.1.0

# Public production origin used for canonical URLs and social previews.
# SITE_URL=https://fieldbook.example.com
